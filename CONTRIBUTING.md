# Contributing

Run `npm install`, then `npm test`. Add a changeset under `.changeset/` for user-facing changes. Keep the canonical contract platform-neutral and update Claude/Codex fixtures together.

# Contributing

Run `npm install`, then `npm test`. Use Conventional Commits: merges to `main` are released automatically by semantic-release. Keep the canonical contract platform-neutral and update Claude/Codex fixtures together.

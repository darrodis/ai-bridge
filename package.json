{
  "name": "ai-bridge",
  "version": "0.1.0",
  "description": "One canonical AI project configuration for Claude Code and Codex",
  "type": "module",
  "bin": { "ai-bridge": "dist/cli.js" },
  "files": ["dist", "README.md", "LICENSE", "schemas"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test",
    "check": "npm run build && npm test",
    "pack:check": "npm pack --dry-run"
  },
  "dependencies": { "picocolors": "^1.1.1" },
  "devDependencies": { "@types/node": "^22.0.0", "typescript": "^5.7.0" },
  "engines": { "node": ">=20" },
  "license": "MIT"
}

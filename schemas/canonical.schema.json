{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ai-bridge canonical metadata",
  "type": "object",
  "properties": {
    "name": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
    "platforms": { "type": "array", "items": { "enum": ["claude", "codex"] } }
  },
  "additionalProperties": true
}
